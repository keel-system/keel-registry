{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://keel.dev/schema/2.0/security.schema.json",
  "title": "Keel Security Layer",
  "description": "Autenticación y autorización: protocolo, catálogos de roles y permisos, y reglas de acceso por operación.",
  "type": "object",
  "required": ["authentication", "access"],
  "additionalProperties": false,
  "properties": {
    "authentication": {
      "type": "object",
      "required": ["protocol"],
      "additionalProperties": false,
      "properties": {
        "protocol": {
          "type": "string",
          "enum": ["oidc", "jwt", "api-key", "none"],
          "description": "Protocolo de autenticación, agnóstico de proveedor (Keycloak, Auth0... se decide al generar)."
        },
        "tokenLocation": { "type": "string", "enum": ["header", "cookie"], "default": "header" },
        "callerIdentity": {
          "type": "object",
          "required": ["field", "from"],
          "additionalProperties": false,
          "description": "De dónde sale, por la superficie HTTP, la identidad de QUIÉN pide el trabajo — no si puede pedirlo (eso es access), sino en nombre de qué recurso lo pide. Es el hermano de messaging.subscriptions.<E>.identity: el mismo hecho por la otra puerta. Sin él, una operación que se resuelve por la credencial deja esa resolución en la prosa de una rule, y cada implementación la inventa: el campo acaba llegando del cuerpo (que lo elige el llamante) o duplicado en dos campos que hay que reconciliar.",
          "properties": {
            "field": {
              "type": "string",
              "pattern": "^[a-z][A-Za-z0-9]*$",
              "description": "Campo del input que recibe la identidad YA RESUELTA. Ese campo deja de viajar en el cuerpo de la petición —lo estampa el servidor, igual que un campo generated—, así que el llamante no puede elegirlo ni hace falta comprobar que no mienta. Debe ser el MISMO campo que usa la identidad del camino de eventos, si la operación entra por las dos puertas: dos campos son dos verdades."
            },
            "from": {
              "type": "object",
              "required": ["source"],
              "additionalProperties": false,
              "description": "Qué del token identifica al recurso. Las dos opciones son legítimas; lo que no lo es es no haberlo decidido.",
              "properties": {
                "source": {
                  "type": "string",
                  "enum": ["serviceClient", "claim"],
                  "description": "'serviceClient': el cliente máquina de la credencial ES el recurso (la entrada de security.serviceClients se corresponde con el identificador del recurso). 'claim': el recurso viaja en un claim del token que puebla el proveedor de identidad."
                },
                "name": {
                  "type": "string",
                  "minLength": 1,
                  "description": "Nombre del claim. Obligatorio con source 'claim' y prohibido con 'serviceClient', donde el identificador es el propio cliente."
                }
              },
              "allOf": [
                {
                  "if": { "properties": { "source": { "const": "claim" } }, "required": ["source"] },
                  "then": { "required": ["name"] }
                },
                {
                  "if": { "properties": { "source": { "const": "serviceClient" } }, "required": ["source"] },
                  "then": { "not": { "required": ["name"] } }
                }
              ]
            },
            "description": { "type": "string" }
          }
        },
        "scoping": {
          "type": "object",
          "required": ["claim", "over", "error"],
          "additionalProperties": false,
          "description": "Alcance por recurso: roles, permissions y scopes son GLOBALES —quien pasa la regla de acceso, pasa para todos los recursos—, así que un servicio multi-inquilino necesita además acotar al recurso concreto. Este bloque declara de dónde sale esa acotación. Sin él, un servicio cuyas operaciones actúan sobre un recurso concreto sirve a cualquier titular del rol los recursos de todos los inquilinos, y el 403 que su diseño promete no lo produce nada.",
          "properties": {
            "claim": {
              "type": "string",
              "pattern": "^[a-zA-Z][a-zA-Z0-9_-]*$",
              "description": "Nombre del claim del token que lleva la lista de recursos a los que el titular alcanza."
            },
            "over": {
              "type": "string",
              "pattern": "^[A-Z][A-Za-z0-9]*\\.[a-z][A-Za-z0-9]*$",
              "description": "Qué identifica al recurso acotado, como Entidad.campo (ej. Application.code). El campo tiene que existir en domain y ser el que las operaciones reciben."
            },
            "error": {
              "$ref": "common.schema.json#/$defs/errorCode",
              "description": "El code con el que se rechaza una petición fuera del alcance. Es contrato público: los escenarios lo afirman y los integradores lo ven."
            },
            "exemptRoles": {
              "type": "array",
              "uniqueItems": true,
              "items": { "$ref": "common.schema.json#/$defs/roleName" },
              "description": "Roles cuya función es transversal y que por tanto NO se acotan (un administrador que gobierna todos los recursos). Se declaran uno a uno: la exención es la parte peligrosa y no puede quedar implícita."
            },
            "description": { "type": "string" }
          }
        },
        "serviceAuth": {
          "type": "object",
          "required": ["protocol"],
          "additionalProperties": false,
          "description": "Autenticación de clientes máquina (endpoints con audience services o both).",
          "properties": {
            "protocol": {
              "type": "string",
              "enum": ["client-credentials", "api-key"],
              "description": "client-credentials (OAuth2) o api-key; el proveedor concreto se decide al generar."
            },
            "validateAudience": {
              "type": "boolean",
              "default": false,
              "description": "Exigir que el claim aud del token incluya la audiencia de este servicio."
            },
            "audience": {
              "type": "string",
              "minLength": 1,
              "description": "Identificador lógico de audiencia; por defecto, el nombre del servicio."
            },
            "description": { "type": "string" }
          }
        },
        "description": { "type": "string" }
      }
    },
    "roles": {
      "type": "object",
      "description": "Catálogo de roles que el servicio reconoce.",
      "propertyNames": { "pattern": "^[a-z][a-z0-9-]*$" },
      "additionalProperties": {
        "type": "object",
        "required": ["description"],
        "additionalProperties": false,
        "properties": {
          "description": { "type": "string", "minLength": 5 }
        }
      }
    },
    "permissions": {
      "type": "object",
      "description": "Catálogo de permisos en formato recurso:accion.",
      "propertyNames": { "pattern": "^[a-z][a-z0-9-]*:[a-z][a-z0-9-]*$" },
      "additionalProperties": {
        "type": "object",
        "required": ["description"],
        "additionalProperties": false,
        "properties": {
          "description": { "type": "string", "minLength": 5 }
        }
      }
    },
    "serviceClients": {
      "type": "object",
      "description": "Catálogo de servicios consumidores (clientes máquina) que este servicio reconoce.",
      "propertyNames": { "pattern": "^[a-z][a-z0-9-]*$" },
      "additionalProperties": {
        "type": "object",
        "required": ["description"],
        "additionalProperties": false,
        "properties": {
          "description": { "type": "string", "minLength": 5 },
          "scopes": {
            "type": "array",
            "description": "Scopes que se conceden a este cliente (deben existir en el catálogo permissions).",
            "items": { "$ref": "https://keel.dev/schema/2.0/common.schema.json#/$defs/permissionName" },
            "minItems": 1
          }
        }
      }
    },
    "roleGrants": {
      "type": "object",
      "description": "Permisos que otorga cada rol.",
      "propertyNames": { "pattern": "^[a-z][a-z0-9-]*$" },
      "additionalProperties": {
        "type": "array",
        "items": { "$ref": "https://keel.dev/schema/2.0/common.schema.json#/$defs/permissionName" },
        "minItems": 1
      }
    },
    "cors": {
      "type": "object",
      "description": "Política de acceso desde navegador (CORS). Su presencia declara que el servicio se consume desde un origen web; los orígenes concretos son dato de despliegue y se configuran por ambiente al generar, no aquí.",
      "required": ["description"],
      "additionalProperties": false,
      "properties": {
        "description": {
          "type": "string",
          "minLength": 10,
          "description": "Quién consume el servicio desde el navegador y por qué."
        },
        "allowCredentials": {
          "type": "boolean",
          "default": false,
          "description": "Permitir cookies y cabeceras de autorización en peticiones cross-origin."
        },
        "allowedHeaders": {
          "type": "array",
          "description": "Cabeceras que el cliente puede enviar; por defecto, todas.",
          "items": { "type": "string", "minLength": 1 },
          "minItems": 1
        },
        "exposedHeaders": {
          "type": "array",
          "description": "Cabeceras de la respuesta que el navegador deja leer al cliente.",
          "items": { "type": "string", "minLength": 1 },
          "minItems": 1
        },
        "maxAgeSeconds": {
          "type": "integer",
          "minimum": 0,
          "default": 3600,
          "description": "Tiempo que el navegador cachea la respuesta al preflight."
        }
      }
    },
    "access": {
      "type": "object",
      "required": ["default"],
      "additionalProperties": false,
      "properties": {
        "default": { "$ref": "https://keel.dev/schema/2.0/common.schema.json#/$defs/accessRule" },
        "rules": {
          "type": "object",
          "description": "Regla de acceso por operación, cuando difiere del default.",
          "propertyNames": { "pattern": "^[a-z][A-Za-z0-9]*$" },
          "additionalProperties": { "$ref": "https://keel.dev/schema/2.0/common.schema.json#/$defs/accessRule" }
        }
      }
    }
  }
}
