---
name: keel-validate
description: Valida un servicio Keel multi-artefacto (schemas por capa + referencias cruzadas vía CLI) y ejecuta la revisión semántica de calidad del diseño. Usar antes de generar código o docs.
---


# /keel-validate — validación estructural, cruzada y semántica

Valida el servicio indicado (directorio `specs/<servicio>/`) en tres niveles. No modifiques los artefactos sin confirmar cada corrección con el usuario, salvo errores triviales de formato.

## Niveles 1 y 2 — Schema por capa + referencias cruzadas (CLI)

```bash
keel validate specs/<servicio>
```

La CLI valida el manifiesto y cada capa contra su schema (`schema/<capa>.schema.json`), la coherencia `layers` ↔ archivos, y las referencias cruzadas mecánicas (tipos, entidades, operaciones, eventos, roles y permisos referenciados existen; agregados bien formados: raíz y miembros existentes, sin entidades en dos agregados, `per-aggregate` solo con agregados declarados; operaciones huérfanas como warning). Además detecta **diseño incompleto**: capas que siguen siendo la plantilla (sin operaciones, entidades, eventos o clientes) y `service.description` placeholder (empieza por `TODO` o es el texto de la plantilla).

Durante una sesión de diseño usa `keel validate --wip specs/<servicio>`: los pendientes de diseño incompleto (los `emits`/`cache.invalidatedBy` hacia una capa messaging aún no diseñada, y los campos `file` cuyo bucket aún no existe en una capa storage aún no diseñada) se reportan como avisos y el veredicto es "Diseño en progreso". El veredicto ✅ **Válido** solo existe sin `--wip`; nunca se genera ni documenta desde un "Diseño en progreso".

Si falla, traduce cada error a lenguaje del DSL (ej. "`use-cases: createProduct.emits`: el evento 'ProductCreated' no está en messaging" → "la operación emite un evento que aún no definiste en la capa messaging") y propón la corrección.

Fallback si el comando `keel` no está disponible: valida cada `<capa>.keel.yaml` con ajv-cli (`--spec=draft2020 -r schema/common.schema.json -s schema/<capa>.schema.json`) y haz las cross-refs leyendo los artefactos.

## Nivel 3 — Semántica (lo que ni el schema ni las cross-refs pueden expresar)

Lee los artefactos y verifica esta checklist. Reporta cada hallazgo con severidad **error** (bloquea generación) o **aviso** (mejorable):

**Consistencia del modelo (error):**
- Cada entidad tiene exactamente un campo con `id: true`.
- Ningún campo `generated: true` o `computed` aparece en el input de una operación.
- `default` de un campo enum pertenece a `values` (inline o del enum nominal).
- Operaciones `query` no tienen `emits`; `cache` solo en queries.
- Ningún campo `sensitive: true` aparece en un output `{ fields }` o payload de evento sin justificación explícita del diseño.
- Los `path` de endpoints con parámetros (`{x}`) usan nombres presentes en el input de la operación.
- Ninguna invariante de una entidad depende de campos de entidades de **otro** agregado (la consistencia entre agregados es eventual, vía eventos).

**Calidad por capa (aviso, salvo indicación):**
- *domain*: invariantes ambiguas o no verificables (cítalas); entidades sin ninguna operación que las use; entidad con campo de estado enum pero sin `lifecycle` (¿las transiciones son realmente libres?); invariantes de texto que en realidad son transiciones y deberían migrar a `lifecycle`; reglas `computed` no derivables de los campos existentes; agregado con muchas entidades internas (¿frontera demasiado grande?); relación `one-to-many` hacia la raíz de otro agregado (¿composición encubierta?); entidad interna con `lifecycle` propio no gobernado por su raíz (cuestiónalo); **prosa que promete un estado que la entidad no puede representar** (**aviso fuerte**): una `description` o una `rule` que dice «no borra el rastro», «se conserva para auditoría», «queda marcada como retirada» sobre una entidad sin ningún campo ni transición que lo sostenga. El implementador solo tiene dos salidas y las dos son malas — borrar de verdad (y la promesa se pierde sin que nada falle) o inventarse el campo (y el diseño deja de describir el servidor) —, así que léelo al revés: por cada verbo de conservación, oculta­ción o marcado de la prosa, señala el campo o la transición que lo sostiene, y si no existe, falta.
- *use-cases*: todo `command` declara al menos un error; command expuesto por HTTP que un cliente con timeout puede reintentar sin `idempotency` **cuyo efecto no sale del proceso** —`keel validate` ya avisa del que publica un evento o encarga trabajo a un proveedor; lo que ninguna regla mecánica ve es el resto: un alta que cobra por dentro, un contador que suma. Comprueba si de verdad hay una clave natural en `persistence` que frene el segundo insert, que es la salida legítima que el DSL no puede mirar, o si nadie lo ha decidido—; `cache.invalidatedBy` no cubre todos los eventos que mutan lo cacheado —`keel validate` ya marca en rojo el caso del `embed` (la entidad embebida sin ninguna vía) y avisa de los eventos de la entidad principal que falten; lo que queda aquí es lo que ninguna regla mecánica ve: vías que **no** son eventos, como una operación de otro servicio o un cambio de configuración—; command que muta entidades de **dos** agregados en una operación (sugerir evento + consistencia eventual); **orden de guardas que no es implementable** (**aviso fuerte**): cuando las `rules` fijan en qué orden se comprueban los rechazos, verifica que cada guarda solo dependa de datos ya disponibles cuando le toca. Una guarda que busca un duplicado «dentro de la aplicación» no puede ir **antes** de la que resuelve esa aplicación: quien lo implemente tendrá que invertirlo, y entonces el orden que el diseño declara —y los escenarios de precedencia que se escribieron contra él— dejan de describir el servidor. Si el orden natural es el otro, decláralo así; si de verdad importa que sea ese, lo que falta es el dato que lo haría posible.
- *api*: output `paginated: true` sin `pagination` declarada; operación expuesta con `DELETE` cuyo `output` no es `"void"` (¿el cliente necesita de verdad ese cuerpo —la operación deja el recurso vecino en un estado que él no puede predecir— o es un `204` que nadie declaró? La combinación imposible, `204` con `output`, ya la da en rojo la CLI; lo que ninguna regla mecánica puede juzgar es si el cuerpo está ahí a propósito).
- *security*: mutaciones con `level: public` (cuestiónalas); roles con permisos que ninguna regla usa (exceso de privilegio); permisos huérfanos; serviceClients con más scopes de los que sus llamadas necesitan (mínimo privilegio también para máquinas); endpoints M2M sin `validateAudience` cuando el servicio convive con otros que comparten servidor de autenticación (un token emitido para otro servicio valdría aquí); **alcance por recurso que la prosa da por hecho y el DSL no puede expresar** (**aviso fuerte**, y `keel validate` ya avisa del caso más visible, el 403 declarado): roles, permissions y scopes son **globales** — quien tiene el rol lo tiene sobre todo. Si el diseño habla de que un editor «solo opera sobre su aplicación», o de que alguien tiene unos recursos «dentro de su alcance», eso hoy no se declara en ninguna parte: hay que decidir y escribir de dónde sale el alcance (un claim del token, una tabla de asignación usuario↔recurso) y aceptar que el generador no lo va a materializar solo. Sin esa decisión, el 403 no lo produce nada, su escenario no es implementable, y el servidor sirve a cualquier titular del rol todos los recursos de todos los inquilinos — que es el fallo de seguridad de verdad, no un detalle de contrato.
- *messaging*: eventos publicados que ninguna operación emite (huérfanos); subscriptions sin `onFailure`; suscripción sobre un canal `external: true` sin `contract` (**aviso fuerte**: el generador supondría la envoltura de Keel sobre un mensaje ajeno); `envelope: keel` sobre un canal `external` (cuestiónalo: solo vale si la fuente también es un servicio Keel); `contract` con `format` avro/protobuf sin `schemaRef` (¿cómo se resuelve el schema?); eventos/suscripciones sin `channel` cuando el servicio se integra con otros (el canal es el contrato de integración y su nombre lógico debe mantenerse estable); nombres de `channel` que filtran tecnología (`kafka`, `queue`, `topic`…) en vez de ser lógicos; **`identity.from` con `location: field`** (la identidad de quien pide el trabajo sale del cuerpo del mensaje) — es una decisión legítima y el schema ya obliga a escribir su asunción en `trustedPublishers`, pero **relee esa asunción cada vez, no la des por vigente**: se sostiene sobre que todos los emisores sean sistemas propios en la misma malla de confianza, y lo que queda vivo es un emisor registrado pidiendo trabajo en nombre de otro sin que nada lo detecte. Cuestiónala si el diseño ya contempla un emisor de fuera del perímetro (un tercero, otra organización, un cliente integrado), si el trabajo que se pide tiene consecuencias que no se pueden retirar —un correo que sale, un cobro— o si hay obligación de auditoría con no-repudio del origen. La salida es `location: header` sobre el principal que estampa el broker, o un destino por emisor con ACL, y cambia dos líneas.
- *http-clients*: `circuitBreaker` sin `fallback`; llamadas sin `timeoutMs`.
- *dependencies*: `replica` cuyo `fedBy` no cubre la **baja o retirada** del recurso en el proveedor (**aviso fuerte**: la copia se queda rancia para siempre y nadie se entera); `onMiss.action: degrade` cuyo `degradedTo` produce datos plausibles pero falsos, indistinguibles para el cliente de una respuesta normal (mismo criterio que el `fallback` de http-clients); `need` con `strategy: on-demand` usado por un `command` transaccional (un timeout deja la transacción abierta); réplica que copia campos que ninguna operación de `usedBy` lee; réplica que se expone tal cual en la salida de una operación o a la que el diseño atribuye invariantes de negocio (**aviso fuerte**: una copia no es fuente de verdad); `contract.version` ausente cuando el proveedor sí publica `INTEGRATION.md` (con una `activation` es **aviso fuerte**: acopla a su contrato de **entrada**, y un campo obligatorio nuevo en su firma rompe este servicio sin que nadie lo toque); dependencia cuyo nombre coincide con el del propio servicio (**error**). Y la coherencia entre capas que `keel validate` ya marca en rojo pero conviene traducir a lenguaje de diseño: **si hay una réplica, tiene que haber capa `persistence`**.
- *dependencies (activaciones)*: **un `need` que en realidad es una activación** — `strategy: on-demand` cuyo `fetchedFrom` apunta a una llamada que muta estado en el proveedor y cuya respuesta no se usa para decidir nada (**aviso fuerte**: el acoplamiento va al revés de como está declarado y queda fuera del mapa del sistema); `activation` dentro de la transacción de escritura de un `command` (un encargo síncrono con la transacción abierta es el mismo problema que un `on-demand`, y además puede dejar trabajo hecho fuera si la transacción luego falla); `onFailure.action: ignore` sobre un trabajo con el que el negocio sí cuenta —pregunta quién lo echaría de menos y cuándo se enteraría— y `awaits: nothing` con el mismo criterio (**aviso fuerte**: la operación responde éxito y el trabajo puede no haberse hecho nunca); `awaits: outcome` que vuelve síncrono un encargo que no necesitaba el desenlace, cuando el diseño ya tiene un evento por el que el resultado podría llegar después; activación por evento sin compensación declarada cuando la operación propia puede fallar **después** de haber encargado el trabajo (**aviso fuerte**: queda trabajo hecho que nadie deshace); **la marca temporal del estado de espera no es la correcta** (**aviso fuerte**): el diseño ya declara CUÁL es —`activations.<a>.awaitingSince`— y `keel validate` comprueba que exista, que sea `timestamp` y que no la gestione la auditoría; lo que no puede ver es si es la correcta, que es lo tuyo. Pregunta **quién la estampa y cuándo**: tiene que escribirla la operación que encarga, justo al encargar, y no tocarla nadie más. Una marca que otra escritura refresca deja la entidad invisible al barrido para siempre, y una que se estampa antes del encargo mide una espera que no había empezado; `effect` genérico o copiado del nombre de la llamada en vez de describir qué provoca de verdad en el proveedor.
- *dependencies (compensaciones)*: `keel validate` ya exige en rojo que la operación de una compensación no pueda aplicarse dos veces y avisa si no devuelve el estado propio; lo que queda aquí es lo que ninguna regla mecánica puede juzgar. **`messageId` que no es estable entre reentregas** (**error**): la CLI comprueba que el campo existe, no que el emisor mande el mismo valor al reentregar. Un id regenerado en cada intento (un uuid nuevo por publicación, un timestamp) deduplica cero y deja la compensación sin ninguna protección con el diseño en verde. Contrástalo con el `INTEGRATION.md` del proveedor: si no lo garantiza, el mecanismo real tiene que ser la transición.
  **Idempotente por declaración pero no por comportamiento** (**error**): la operación se protege solo con `contract.messageId` —que deduplica el mensaje, no el efecto— y su cuerpo hace algo que sí es sensible a repetirse por otra vía (emite un evento incondicionalmente, suma en vez de fijar, llama a un tercero); el mecanismo elegido tiene que cortar el efecto **real**, no la entrega. **Transición de vuelta que existe pero no es la correcta** (**aviso fuerte**): la CLI comprueba que la arista está declarada en `lifecycle`, no que sea el estado al que el negocio quiere volver — un pedido cuya reserva se revierte no siempre vuelve a `pending`, a veces va a `cancelled`, y devolverlo al estado equivocado lo hace reintentable cuando no debería. **Compensación que deja la entidad en un callejón sin salida** (**error**): `keel validate` ya avisa cuando el destino de la vuelta es un estado terminal del `lifecycle`, pero solo pone el dato —un terminal puede ser el desenlace correcto—; lo que hay que juzgar aquí es si ese final es el que el negocio quiere o un callejón creado por el camino de excepción, que es el sitio donde menos se mira.
- *persistence*: entidades de domain no mencionadas (¿se persisten o no?); campos `unique` o de queries frecuentes sin índice; `per-operation` habiendo agregados declarados cuyas operaciones tocan raíz + internas (¿debería ser `per-aggregate`?).
- *storage*: buckets sin `maxSizeMb` (¿subida sin límite de tamaño?); bucket con datos personales o documentos privados marcado `public` (**cuestiónalo**); bucket declarado que ningún campo `file` referencia (huérfano); operación que sube a un bucket sin declarar los errores de subida esperados (`FILE_TOO_LARGE`, `UNSUPPORTED_CONTENT_TYPE`) en use-cases.
- *mail*: operación en `sentBy` sin `idempotency` ni transición (**cuestiónalo**: un correo que sale no lo deshace ninguna transacción, y un reintento lo manda dos veces a una persona real); `sender.source: data` sin `fallback` (¿se prefiere no enviar antes que enviar desde una dirección sin verificar, o es un olvido?); `html` sin `text` (los filtros antispam lo penalizan y no falla en ninguna prueba); `templating.source: data` sin `declaredVariables` (una variable que falte se interpolará vacía y el correo saldrá con un hueco donde iba el importe); operación que manda correo y no declara error para «no hay plantilla» ni para «falta una variable obligatoria»; y la pregunta que casi nunca está escrita: si la operación **responde antes de enviar** (un `202`), ¿qué se hace con el envío que falla después? — sin un desenlace declarado, el correo se pierde y nadie se entera. Y su hermana, un paso más allá: si alguna regla del diseño depende de un fallo que ocurre **después** de que el relay aceptara el mensaje —clasificar un rebote duro, suprimir la dirección que rebotó, contar quejas—, ¿**por qué canal llega esa noticia**? El relay acepta y responde OK; el rebote vuelve minutos u horas después, por un webhook o un buzón de retorno, y si el diseño no lo declara esa regla no es implementable ni verificable: ninguna infraestructura de prueba rebota sola, así que sus escenarios quedan sin ejercitar y el código se escribe adivinando. La forma de la decisión ya existe en el DSL, en `dependencies` (`awaits`, `reconciledBy`, `unansweredAfterSeconds`): un desenlace que llega tarde o no llega. Si no hay canal, dilo como hueco declarado en vez de escribir reglas que dependen de él.

**Escenarios de validación (`validation-scenarios.md`):**
- Existe `specs/<servicio>/validation-scenarios.md` (formato: `docs/validation-scenarios.md`); si falta, **error**: el diseño no está cerrado y el generador no puede validar el servidor.
- Su matriz de cobertura incluye toda operación de use-cases, y cada `error` declarado aparece en algún flujo o caso borde con su `code` exacto (huecos: **error**).
- Rutas, payloads, estados y eventos de los escenarios coinciden con los artefactos; si el spec cambió después del archivo (discrepancias o versión distinta en su cabecera), márcalo como desactualizado (**error**) y propón regenerarlo con `/keel-design`.
- El archivo es el **contrato de equivalencia** entre implementaciones: lo que no fija, cada generador lo decide por su cuenta. Comprueba contra `docs/validation-scenarios.md § Determinación observable` (el criterio vive allí, no lo dupliques aquí) y marca como **error** los tres fallos que lo vacían de contenido: `Then` que solo verifica el status en vez del cuerpo completo de la respuesta; error cubierto sin su status HTTP; flujo cuyo `Given` depende de la ejecución de otro flujo (tras el reset previo a cada flujo, ese estado no existe). Como **aviso**: colección devuelta sin orden declarado, estado del `lifecycle` que ningún flujo alcanza, y `cache.invalidatedBy` con vías no ejercitadas.
- **Las enumeraciones de campos coinciden entre sí y con el artefacto** (**error**). Deriva la proyección de cada `output` —campos de la entidad, menos `exclude`, más los objetos de `embed`, más los campos con `default`— y contrástala contra **cada** `Then` que enumere esa entidad. Dos escenarios del mismo documento que discrepan sobre si un campo viaja obligan a quien genere a elegir a cuál obedecer, y esa elección no es suya. El caso más frecuente: un campo con `default` que no aparece en la petición, se olvida en la respuesta de creación y se da por supuesto en el flujo de transición de estado.
- **Toda caché tiene un escenario de retención** (**aviso fuerte**): una lectura que, dentro del TTL y tras una mutación que **no** está en `invalidatedBy`, sigue devolviendo el valor viejo. Sin él, una implementación que no cachea absolutamente nada pasa todos los escenarios de invalidación.
- **Ningún escenario exige lo que el diseño no puede cumplir** (**error**). El caso concreto: un `Then` que espera ver un cambio reflejado de inmediato en un objeto `embed` cuya entidad no aporta ningún evento a `invalidatedBy`. No es un fallo del futuro servidor: es una contradicción entre dos artefactos del mismo diseño, y se resuelve en el YAML o en el escenario, nunca en el código.
- **`security.cors` sin sus dos escenarios** (**aviso**): con la política declarada, el archivo debe traer un escenario de **preflight** (`OPTIONS` sin credencial, con el método y las cabeceras que la política admite) **y** uno de petición normal cross-origin que afirme las cabeceras de `exposedHeaders`. Con solo el primero, el servidor contesta al preflight y el navegador sigue sin poder leer la respuesta: son dos fallos distintos, no dos redacciones del mismo.
- **`onUnavailable` sin escenario del proveedor caído** (**aviso fuerte**): no lo cubre el de `onMiss`, que habla de la copia vacía y no de la indisponibilidad. Con `lastKnown` son dos, y el que falta casi siempre es el del **arranque en frío** —nunca hubo lectura previa, así que la operación falla con el `error` declarado—: sin él, un almacén que no llega a guardar nada pasa el escenario del rescate y la política degrada en silencio a «fallar siempre».

## Salida

Termina con un veredicto claro:
- ✅ **Válido** — listo para `keel-<tech> build specs/<servicio>` (que genera el proyecto; el código se completa después con `/keel-generate-<tech>` dentro de él) y para `/keel-docs`.
- ❌ **Inválido** — lista numerada de errores (y avisos aparte), cada uno con el artefacto afectado y su corrección propuesta. Ofrece aplicarlas.
